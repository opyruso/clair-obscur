name: Déploiement Recette

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Lancer le déploiement
    runs-on: self-hosted

    steps:
      - name: 🧬 Récupération du code
        uses: actions/checkout@v4

      - name: 🚀 Exécution du script de déploiement
        run: |
          chmod +x ./.github/deploy-recette.sh
          ./.github/deploy-recette.sh
